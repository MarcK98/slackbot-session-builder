{
  "name": "bolt-js-getting-started-app",
  "version": "2.0.0",
  "description": "Getting Started ⚡️ Bolt for JavaScript",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild ./src/index.js  --bundle --outfile=./dist/build.cjs --format=cjs --platform=node"
  },
  "license": "MIT",
  "dependencies": {
    "@slack/bolt": "^3.6.0",
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "webpack-cli": "^5.1.4"
  }
}
